server:
  url: "http://localhost:8000"
  token: "default_token" # Replace this with the enrollment token generated from the Settings tab
interval: 10
heartbeat_interval: 5
uuid_file_path: "device.uuid"
checks:
  firewall: true
  encryption: true
  updates: true
  ssh: true
  runtime: true
