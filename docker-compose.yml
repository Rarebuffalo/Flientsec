version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: flientsec-db
    restart: always
    network_mode: host
    environment:
      POSTGRES_USER: flientsec
      POSTGRES_PASSWORD: flientsec_dev_pass
      POSTGRES_DB: flientsec
    command: -p 5433
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: flientsec-backend
    restart: always
    network_mode: host
    environment:
      DATABASE_URL: postgresql://flientsec:flientsec_dev_pass@localhost:5433/flientsec
      JWT_SECRET: super_secret_jwt_key_for_dev_123
      ACCESS_TOKEN_EXPIRE_MINUTES: 1440
      ENV: dev
    depends_on:
      - db

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: flientsec-frontend
    restart: always
    network_mode: host
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8000
    depends_on:
      - backend

volumes:
  postgres_data:

